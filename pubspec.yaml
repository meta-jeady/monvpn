name: kco4p_vpn
description: KČØ4P VPN App

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_v2ray: ^1.0.8
  shared_preferences: ^2.2.3
  fl_chart: ^0.68.0   # <- AJOUTE CETTE LIGNE

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
